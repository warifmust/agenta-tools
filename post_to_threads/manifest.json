{
  "name": "post_to_threads",
  "description": "Publish a text post to Threads via the Meta Graph API (two-step: create a TEXT container, poll until ready, then publish). Requires curl + jq on the host; set THREADS_USER_ID and THREADS_ACCESS_TOKEN in the environment. Carries no secrets itself.",
  "handler": "post_to_threads.sh",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "text": {
        "type": "string",
        "description": "The post text (max 500 characters)."
      }
    },
    "required": ["text"]
  },
  "env": ["THREADS_USER_ID", "THREADS_ACCESS_TOKEN"],
  "side_effect": "write"
}
