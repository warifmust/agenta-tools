{
  "description": "Fetch and parse an RSS or Atom feed, returning the most recent items as JSON (title, link, published). Python stdlib only. Requires python3.",
  "env": [],
  "handler": "rss_fetch.py",
  "name": "rss_fetch",
  "parameters": {
    "properties": {
      "limit": {
        "default": 5,
        "type": "integer"
      },
      "url": {
        "type": "string"
      }
    },
    "required": [
      "url"
    ],
    "type": "object"
  },
  "side_effect": "read_only",
  "requires": [
    "python3"
  ]
}
